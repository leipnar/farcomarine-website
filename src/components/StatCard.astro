---
export interface Props {
  value: string;
  label: string;
}
const { value, label } = Astro.props;
---

<div class="text-center px-4">
  <div class="text-2xl sm:text-3xl font-bold text-white mb-1">{value}</div>
  <div class="text-ocean-400/80 text-sm">{label}</div>
</div>
